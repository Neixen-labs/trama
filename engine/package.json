{
  "name": "@trama/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "tsx --test test/*.test.ts",
    "check": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "demo": "npm run build && node demo/serve.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "maplibre-gl": "^6.2.0",
    "tsx": "^4.23.1",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "fzstd": "^0.1.1"
  }
}
