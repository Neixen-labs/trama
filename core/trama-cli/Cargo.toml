[package]
name = "trama-cli"
description = "The TRAMA command line: compile, validate, export."
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "trama"
path = "src/main.rs"

[dependencies]
trama-format = { path = "../trama-format" }
trama-epanet = { path = "../trama-epanet" }
trama-swmm = { path = "../trama-swmm" }
trama-roads = { path = "../trama-roads" }
serde_json.workspace = true
clap = { version = "4", features = ["derive"] }
rusqlite = { version = "0.40.2", features = ["bundled"] }

[dev-dependencies]
trama-format = { path = "../trama-format" }
# The GeoPackage test reads the file back through SQLite rather than through the writer.
rusqlite = { version = "0.40.2", features = ["bundled"] }
